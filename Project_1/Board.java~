import java.util.Random;

public class MemoryPuzzle{

    private HashMap board;

    public MemoryPuzzle(size){
        LinkedList temp;
        for(int i=0;i<size;i++){
            for(int j=0;j<size;j++){
                temp.append(randCards.pop());
            }
            board.append(temp);
        }

